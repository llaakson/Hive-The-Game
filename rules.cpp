#include "Hive.hpp"


bool isMoveAllowed()
{
	if (turn_counter == 0)
		return true;
	else (turn_counter == 1)
		// check if neibourgs opponent)
	//else if turn 4 and no queen - queen	
	else
	{
		//if getting placed - has same color neighbour
		//pieces specific logic
	}
}
